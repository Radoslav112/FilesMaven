package features.stringCalculatorFeature.helpers;

public enum Error {
        NO_ERROR,
        MISSED_NUMBER_END_LINE_FOUND,
        MISSED_NUMBER_COMMA_FOUND,
        MISSED_NUMBER_CUSTOM_SEP_FOUND,
        NEGATIVE_NOT_ALLOWED,
        UNEXPECTED_CHAR,
        UNEXPECTED_EOF
}